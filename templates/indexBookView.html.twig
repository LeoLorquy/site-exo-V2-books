
{% extends 'base.html.twig' %}
{% block  metatitle %}market+ accueil{% endblock %}
{% block content %}
    <article class="book-detail">
{#        <div class="book-image">#}
{#            <img class="book-image" src="{{ book.image }}" title="{{ book.title }}" alt="photo de la couverture du livre">#}
{#        </div>#}
<br><br><br><br><br>
        <div class="card-text">
            <center><h2>{{ book.title }}</h2>
            {% if book.author %}
                <h3>{{ book.author.firstName }} {{ book.author.lastName }}</h3>
            {% endif %}
            <div class="date"></div>
                <em>Date de publication : {{ book.publishedAt | date('d/m/y') }}</em>
            </div>
            </center>
        </div>
    </article>
    <br><br><br><br>
{% endblock content %}